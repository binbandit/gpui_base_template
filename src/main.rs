//! Your app starts here. The showcase implementation lives in `src/app/`.
//! Run `./setup.sh <name> --minimal` for a one-file, bare-GPUI starter.

use gpui_base_framework::run;

fn main() -> anyhow::Result<()> {
    run()
}
